"""
# 二分查找变种(variants)
"""
import os
import logging

logger = logging.getLogger(__name__)


def binary_search_left(arr: list, target):
    """变种1: 查找数组中第一个等于目标值的索引号"""
    pass 


if __name__ == '__main__':
    logging.basicConfig(format="[%(asctime)s %(filename)s:%(lineno)s] %(message)s",
                        level=logging.INFO,
                        filemode="a",
                        filename=None)
